CREATE TABLE Users (SYNumber INT PRIMARY KEY, 
					FirstName VarChar(20), 
					LastName VarChar(20), 
					Age INT, 
					PhotoPath VarChar(100),
					Address VarChar(20),
					UserType Varchar(20)
);
